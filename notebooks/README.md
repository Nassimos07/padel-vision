# Notebooks

Narrative analysis notebooks, one per stage. They tell the story behind the code
in `src/` and produce the figures used in write-ups.

- `01_object_detection.ipynb` — Stage 1: detecting players and the ball, exploring
  confidence thresholds and per-frame detection counts. _(added after the first
  detection run)_

> Tip: keep heavy outputs out of git — clear notebook outputs before committing,
> or rely on the `.ipynb_checkpoints` ignore rule already in `.gitignore`.
