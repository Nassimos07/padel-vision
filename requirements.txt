# ---------------------------------------------------------------------------
# Install PyTorch FIRST with a CUDA build that matches your driver, e.g.:
#   CUDA 12.1:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   CPU only :  pip install torch torchvision
# Then:  pip install -r requirements.txt
# (or the editable install:  pip install -e ".[notebook,dev]")
# ---------------------------------------------------------------------------

# Core (the padel-vision package)
rfdetr>=1.0
ultralytics>=8.3.0
supervision>=0.25.0
opencv-python>=4.9.0
numpy>=1.26
pyyaml>=6.0
tqdm>=4.66
fire>=0.6

# Notebook tutorials
jupyter>=1.0
ipywidgets>=8.1
matplotlib>=3.8
pandas>=2.1
